# Family Tree Website - Development Instructions for Claude Code

## Project Overview

Build a modern, responsive family tree website using React, JavaScript, Tailwind CSS, Firebase, and Firestore. The application should allow users to create, view, and manage family relationships with an intuitive interface.

## Technology Stack

- **Frontend**: React 18+ with JavaScript
- **Styling**: Tailwind CSS 3+
- **Database**: Firebase Firestore
- **Authentication**: Firebase Auth
- **Hosting**: Firebase Hosting
- **Testing**: Jest, React Testing Library, Cypress (E2E)
- **Build Tool**: Vite
- **State Management**: React Context + useReducer

## Core Requirements

### 1. Project Setup & Structure

```
family-tree-app/
├── src/
│   ├── components/
│   │   ├── common/
│   │   ├── auth/
│   │   ├── tree/
│   │   └── forms/
│   ├── contexts/
│   ├── hooks/
│   ├── services/
│   ├── utils/
│   ├── pages/
│   └── __tests__/
├── public/
├── cypress/
└── firebase.json
```

### 2. Firebase Configuration

- Set up Firebase project with Firestore and Authentication
- Configure Firebase SDK with environment variables
- Implement proper security rules for Firestore
- Enable email/password and Google authentication

### 3. Core Features to Implement

#### Authentication System

- User registration and login
- Protected routes
- User profile management
- Password reset functionality

#### Family Tree Management

- Add/edit/delete family members
- Define relationships (parent, child, spouse, sibling)
- Upload and manage photos
- Add biographical information (birth date, death date, location, notes)

#### Tree Visualization

- Interactive family tree diagram
- Zoom and pan functionality
- Multiple view options (ancestor view, descendant view, family view)
- Responsive design for mobile and desktop

#### Data Management

- Real-time synchronization with Firestore
- Data validation and error handling
- Import/export family data
- Search and filter functionality

### 4. Development Best Practices

#### Code Quality

- Use ESLint and Prettier for code formatting
- Implement TypeScript-style JSDoc comments
- Follow React best practices (hooks, functional components)
- Use custom hooks for reusable logic
- Implement proper error boundaries

#### Performance

- Implement React.memo for expensive components
- Use React.lazy for code splitting
- Optimize Firestore queries with proper indexing
- Implement image optimization and lazy loading

#### Security

- Validate all user inputs
- Implement proper Firestore security rules
- Sanitize data before display
- Use Firebase Auth for secure authentication

#### Testing Strategy

- Unit tests for utility functions and hooks
- Component tests with React Testing Library
- Integration tests for Firebase operations
- End-to-end tests with Cypress for critical user flows

### 5. Implementation Process

#### Phase 1: Foundation

1. Initialize React app with Vite
2. Set up Tailwind CSS configuration
3. Configure Firebase SDK and environment variables
4. Implement basic routing with React Router
5. Create authentication context and hooks
6. Set up testing framework

**Testing Requirements for Phase 1:**

- Test Firebase configuration connection
- Verify routing works correctly
- Test authentication flow (login/logout/register)
- Run initial component tests

#### Phase 2: Core Features

1. Implement user authentication UI
2. Create family member management forms
3. Build basic family tree data structure
4. Implement CRUD operations for family members
5. Create simple tree visualization

**Testing Requirements for Phase 2:**

- Test all form validations
- Verify Firestore operations (CRUD)
- Test data persistence and retrieval
- Check responsive design on different screen sizes

#### Phase 3: Advanced Features

1. Implement interactive tree visualization
2. Add photo upload functionality
3. Create advanced search and filtering
4. Implement data import/export
5. Add sharing and collaboration features

**Testing Requirements for Phase 3:**

- Test tree rendering with complex family structures
- Verify image upload and storage
- Test search functionality with various queries
- Check import/export data integrity

#### Phase 4: Polish & Optimization

1. Optimize performance and loading times
2. Implement offline capabilities
3. Add accessibility features (ARIA labels, keyboard navigation)
4. Create comprehensive error handling
5. Add analytics and monitoring

**Testing Requirements for Phase 4:**

- Performance testing with large datasets
- Accessibility testing with screen readers
- Cross-browser compatibility testing
- Mobile device testing

### 6. Iterative Development Process

For each feature or component:

1. **Plan**: Break down the feature into small, testable units
2. **Implement**: Write the code following best practices
3. **Test**: Run relevant tests (unit, integration, E2E)
4. **Debug**: Fix any failing tests or issues
5. **Refactor**: Improve code quality and performance
6. **Re-test**: Ensure all tests pass after changes
7. **Deploy**: Update Firebase hosting with changes

### 7. Testing Commands and Scripts

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "deploy": "firebase deploy"
  }
}
```

### 8. Key Components to Build

#### Essential Components

- `AuthProvider` - Authentication context
- `FamilyTreeProvider` - Family data context
- `TreeVisualization` - Interactive tree display
- `PersonCard` - Individual family member display
- `PersonForm` - Add/edit family member
- `RelationshipForm` - Define relationships
- `SearchBar` - Search functionality
- `PhotoUpload` - Image handling
- `ErrorBoundary` - Error handling
- `LoadingSpinner` - Loading states

### 9. Error Handling & Testing Strategy

#### Must Test Scenarios

- Authentication flows (success and failure)
- Form validation with invalid inputs
- Network connectivity issues
- Large family tree rendering
- Image upload failures
- Data synchronization conflicts
- Permission-based access control

#### Continuous Testing Loop

After implementing each feature:

1. Run unit tests (`npm test`)
2. Check component rendering (`npm run test:coverage`)
3. Verify Firebase operations work in development
4. Test responsive design on multiple devices
5. Run E2E tests for critical paths
6. Fix any issues found
7. Repeat until all tests pass

### 10. Firebase Security Rules Template

```javascript
// Firestore Security Rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /families/{familyId} {
      allow read, write: if request.auth != null &&
        request.auth.uid in resource.data.members;
    }
    match /persons/{personId} {
      allow read, write: if request.auth != null &&
        request.auth.uid == resource.data.createdBy;
    }
  }
}
```

### 11. Success Criteria

The project is complete when:

- All authentication flows work without errors
- Users can create and manage family trees
- Tree visualization renders correctly on all devices
- All tests pass (unit, integration, E2E)
- Application loads quickly (<3 seconds initial load)
- No console errors or warnings in production
- Accessibility standards are met (WCAG 2.1 AA)
- Firebase security rules properly protect user data

### 12. Deployment Checklist

Before deploying:

- [ ] All tests pass
- [ ] No console errors in production build
- [ ] Firebase security rules are properly configured
- [ ] Environment variables are set correctly
- [ ] Performance metrics meet requirements
- [ ] Accessibility testing completed
- [ ] Cross-browser testing completed
- [ ] Mobile responsiveness verified

## Instructions for Claude Code

When working on this project:

1. **Start with setup**: Initialize the Next.js project structure and configure all tools
2. **Build incrementally**: Implement one feature at a time, starting with API routes
3. **Test everything**: After each change, run the appropriate tests including API testing
4. **Use Next.js features**: Leverage App Router, API routes, SSR/SSG, and Image optimization
5. **Fix issues immediately**: Don't move to the next feature until current one works
6. **Iterate until perfect**: Keep testing and fixing until all functionality works as expected
7. **Follow Next.js best practices**: Use proper patterns, file-based routing, and performance optimizations
8. **Document decisions**: Add comments explaining complex logic or architectural choices

Remember: The goal is a production-ready Next.js application that real families can use to preserve their history and relationships, with excellent performance, SEO, and user experience.
